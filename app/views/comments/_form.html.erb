<div class="comment-form hidden">
  <%= semantic_form_for [job, Comment.new], :remote => true do |f| %>
    <p class="comment-body">
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit %>
    </p>
  <% end %>
</div>